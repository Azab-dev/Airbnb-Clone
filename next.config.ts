import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains:["links.papareact.com" ,"cdn.prod.website-files.com" , "links.papareact.com"]
  },
};

export default nextConfig;
